import { AuthImagePipePipe } from './auth-image-pipe.pipe';

describe('AuthImagePipePipe', () => {
  it('create an instance', () => {
    const pipe = new AuthImagePipePipe();
    expect(pipe).toBeTruthy();
  });
});
